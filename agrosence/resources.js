import Signup from './src/resources/Signup.png';

// initialize Array which contain src and alt of images where each image is an object
const imageDetails = {
    Signup: {
        src: Signup,
        alt: 'Sign in illustration',
    },
}

// export the imageDetails object
export { imageDetails };