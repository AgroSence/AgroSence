import Singup_bg from './src/resources/Signup.png';

// initialize Array which contain src and alt of images where each image is an object
let images = [
    {
        src: Singup_bg,
        alt: 'Sign in illustration',
    },
]
export default images;